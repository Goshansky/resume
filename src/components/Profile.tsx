import profilePic from '../images/ava.jpg';

const Profile = () => {
    return (
        <div className="resume_profile">
            <img src={profilePic} alt="profile_pic"/>
        </div>
    );
};

export default Profile;